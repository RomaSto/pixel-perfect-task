mixin nav(className, items, activeIndex)
    nav(class=className)
        .container
            ul(class=className + "__list")
                each item, ndx in items
                    li(class=className + "__item" + ((ndx == activeIndex) ? ' nav__item_active' : ""))
                        a(class=className + "__link")= item